# 安全策略

## 报告安全问题

**请不要在公开 Issue 中提交安全漏洞或任何凭据。**

优先使用 GitHub 的私密通道：

- 打开 <https://github.com/zzyyyds88/PowerBarRations/security/advisories/new>（Security → Report a vulnerability）

如果无法使用上述入口，请发邮件给维护者，并在标题前置 `[SECURITY]`。我们会在可行范围内尽快确认并回复。

请在报告中包含：

- 受影响的版本 / commit；
- 复现步骤或最小验证；
- 影响范围（是否能读他人数据、绕过鉴权、泄露密钥等）；
- 你已知的缓解方式。

**请勿**在报告里粘贴真实的管理密钥、客户端密钥、渠道 key 或用户数据；用占位符描述即可。

## 支持范围

本项目是**单用户、自建的网关**，只有 `main` 分支的当前版本接受安全修复。历史 commit、未合并的 fork、以及自行改动过的部署不在维护范围内。

## 部署方需自行承担的边界

设计上，本项目**只提供明文 HTTP**、内存中保存运行态、且管理密钥由登录口令单次 SHA256 派生（无盐、无 KDF 迭代）。因此：

- 口令必须是**长随机串**（建议 ≥32 字符）；
- 跨机访问必须由外部反向代理终结 TLS（见 README §5.1），或把 `PBR_BIND` 收成 `127.0.0.1`；
- 渠道 key 与客户端密钥会明文经过本服务，不要把明文端口直接暴露到不可信网络。

这些是文档化的设计取舍，不是漏洞；但若你发现它们能被用来**越权**，请照上面的方式报告。

## 我们不接受的"漏洞"

- "示例口令/示例域名可被登录"——仓库里不含任何真实凭据，示例值必须由部署方替换；
- 需要先拿到管理密钥或物理访问机器才能触发的行为；
- 缺少与本项目定位无关的通用加固（如多用户 RBAC、计费反作弊）。
